# Carbon Environment Configuration
# Copy this file to .env and customize as needed
# Usage: Copy to .env, then: docker compose up -d
#        Or: source .env && ./start-carbon-gpu.sh

# ============================================
# SECURITY & AUTHENTICATION
# ============================================

# User credentials
CARBON_USER=carbon
CARBON_PASSWORD=Carbon123#

# VNC password (for VNC client access)
VNC_PASSWORD=Carbon123#

# code-server password (for browser-based VS Code)
CODE_SERVER_PASSWORD=Carbon123#

# Database passwords
POSTGRES_PASSWORD=Carbon123#
MONGODB_PASSWORD=Carbon123#
REDIS_PASSWORD=

# ============================================
# USER SETTINGS
# ============================================

# User ID and Group ID (match your host user for file ownership)
# Find yours with: id -u (UID) and id -g (GID)
CARBON_UID=1000
CARBON_GID=100

# Timezone (see: timedatectl list-timezones)
TZ=Australia/Brisbane

# ============================================
# WORKSPACE LOCATIONS
# ============================================

# Host directory mounted to /work in container
# This is your main workspace for projects
WORK_DIR=./work

# Host directory mounted to /data in container
# This stores database data for persistence
DATA_DIR=./data

# Host directory mounted to /home/carbon in container (optional)
# Leave empty to use container's internal home directory
HOME_DIR=

# ============================================
# DISPLAY SETTINGS
# ============================================

# VNC resolution (e.g., 1920x1080, 2560x1440, 3840x2160)
VNC_RESOLUTION=1920x1080

# VNC color depth (16, 24, or 32)
VNC_DEPTH=24

# VNC display number (usually :1)
VNC_DISPLAY=:1

# ============================================
# PORT MAPPINGS
# ============================================

# RDP (Remote Desktop Protocol)
RDP_PORT=3390

# noVNC (Web-based VNC access)
NOVNC_PORT=6900

# VNC (Direct VNC client access)
VNC_PORT=5900

# PostgreSQL
POSTGRES_PORT=5432

# MongoDB
MONGODB_PORT=27017

# Redis
REDIS_PORT=6379

# Jupyter Lab (compute image only)
JUPYTER_PORT=8888

# code-server (compute image only)
CODE_SERVER_PORT=9999

# Spark Master UI (compute image only)
SPARK_MASTER_UI_PORT=8080

# Spark Worker UI (compute image only)
SPARK_WORKER_UI_PORT=8081

# Ollama API (GPU images only)
OLLAMA_PORT=11434

# Qdrant Vector Database
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334

# SSH Server
SSH_PORT=2222

# ============================================
# SERVICE TOGGLES
# ============================================

# Enable/disable services (true/false)
# Note: Databases are OFF by default to save resources
# Enable only what you need!

# Development Tools (default: ON)
ENABLE_JUPYTER=true
ENABLE_CODE_SERVER=true
ENABLE_SPARK=true

# Databases (default: OFF)
ENABLE_POSTGRESQL=false
ENABLE_MYSQL=false
ENABLE_MONGODB=false
ENABLE_REDIS=false
ENABLE_MOSQUITTO=false

# Vector Databases (default: OFF)
ENABLE_QDRANT=false

# AI/ML Services (default: OFF)
ENABLE_OLLAMA=false
ENABLE_VLLM=false

# Desktop (default: ON)
ENABLE_VNC=true
ENABLE_XRDP=true

# SSH Server (default: ON)
ENABLE_SSH=true

# ============================================
# RESOURCE LIMITS
# ============================================

# Memory limit (e.g., 8g, 16g, 32g)
MEMORY_LIMIT=16g

# CPU limit (e.g., 4, 8, 16)
CPU_LIMIT=8

# GPU device selection (e.g., 0, "0,1", all)
# Use "all" for all GPUs, "0" for first GPU, "0,1" for multiple
GPU_DEVICES=all

# ============================================
# ADVANCED SETTINGS
# ============================================

# Container name
CONTAINER_NAME=carbon-gpu

# Image to use
CARBON_IMAGE=wisejnrs/carbon-base:latest-gpu

# Network mode (bridge, host, none)
NETWORK_MODE=bridge

# Restart policy (no, always, unless-stopped, on-failure)
RESTART_POLICY=unless-stopped

# Enable privileged mode (needed for some Docker-in-Docker scenarios)
PRIVILEGED=false

# Desktop theme (arc-dark, nordic, mint-y-dark)
DESKTOP_THEME=arc-dark

# Icon theme (papirus-dark, numix-circle, yaru)
ICON_THEME=papirus-dark

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# ============================================
# JUPYTER SETTINGS (compute image)
# ============================================

# Jupyter token (leave empty to auto-generate)
JUPYTER_TOKEN=

# Jupyter enable lab (true/false)
JUPYTER_ENABLE_LAB=true

# ============================================
# SPARK SETTINGS (compute image)
# ============================================

# Spark master URL
SPARK_MASTER=local[*]

# Spark driver memory
SPARK_DRIVER_MEMORY=4g

# Spark executor memory
SPARK_EXECUTOR_MEMORY=4g
